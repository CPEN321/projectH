<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/action_add_event"
        android:checkable="false"
        android:enabled="true"
        android:icon="@drawable/baseline_queue_black_36dp"
        android:title="Item"
        android:visible="true"
        app:showAsAction="always" />
    <item
        android:id="@+id/action_calendar"
        android:icon="@drawable/baseline_calendar_today_black_36"
        android:title="Calendar"
        app:showAsAction="always" />

    <!-- "Mark Favorite", should appear as action button if possible -->
    <item
        android:id="@+id/action_chats"
        android:icon="@drawable/baseline_message_black_36"
        android:title="Chats"
        app:showAsAction="always" />

    <!-- Group Calendar, should always be in the overflow -->
    <item
        android:id="@+id/action_group_calendar"
        android:title="Group Calendar"
        app:showAsAction="never" />

    <!-- Settings, should always be in the overflow -->
    <item
        android:id="@+id/action_settings"
        android:title="Log Out"
        app:showAsAction="never" />

</menu>